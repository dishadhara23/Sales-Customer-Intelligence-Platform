# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
*.egg-info/

# Data — raw CSVs are downloaded by scripts/download_data.py, not committed.
data/raw/*.csv
data/processed/*.db
data/processed/*.parquet

# Tableau extracts ARE committed — they are small (~250 KB total) and make the
# workbook usable straight after a clone, with no pipeline run needed first.
dashboards/tableau/extracts/*.hyper
*.twbr
*.twb.bak

# OS
.DS_Store

# pytest
.pytest_cache/
